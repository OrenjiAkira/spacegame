
void PressStart_load();
/* () -> void
Carrega as entidades da cena de press start (imagem de fundo, texto). */

void PressStart_pause();
/* () -> void
Faz nada. Ta aí pra não deixar a struct vazia. */

void PressStart_close();
/* () -> void
Encerra e libera memória das entidades da cena de press start. */
