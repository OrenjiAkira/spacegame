#ifndef GAME_STRING_H
#define GAME_STRING_H

void String_join(char* target, char* const str1, char* const str2);
/* (char*, char* const, char* const) -> void
Concatena duas strings (str1 e str2) numa só (target). */

#endif
